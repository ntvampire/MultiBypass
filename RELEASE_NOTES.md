- 🎨 **Обновленный дизайн иконки**: строгий, чистый плоский Material 3 дизайн без градиентных переливов и неонового свечения, с сохранением узнаваемой геометрии маршрутов обхода и центрального щита.
- 📐 **Стратегии группы 2 (Анти-DPI)**: список пресетов и файл автоматического тестирования упорядочены строго от простых к сложным для более быстрого подбора наименее инвазивных правил.
- 📱 **Улучшенный выбор приложений**: в списках обеих групп исключены системные фоновые процессы без пользовательского интерфейса (`getLaunchIntentForPackage`), а уже выбранные приложения автоматически закрепляются в самом верху списка при открытии.
- 🛠️ **Стабильность автоподбора стратегий**: устранено аварийное завершение процесса при запуске автоподбора — реализована строгая межпоточная синхронизация нативного C-движка и контроллера, единый пул HTTP-клиента и временная пауза сторожевого сервиса на период тестирования.
