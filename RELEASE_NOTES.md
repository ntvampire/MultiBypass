- 🎨 **Иконка приложения**: исправлено отображение иконки в лаунчере Android (устранено обрезание до 1/4, добавлены правильные адаптивные слои для всех плотностей экранов от mdpi до xxxhdpi).
- 🛡️ **DNS-over-HTTPS (DoH) на мобильном интернете**: реализован встроенный локальный DoH-мост (`127.0.0.1:5353`), преобразующий UDP DNS-запросы от приложений туннеля в шифрованный трафик HTTPS (RFC 8484) с автономным bootstrap-разрешением. Обходит блокировки и перехват UDP 53 операторами сотовой связи (МТС, Мегафон, Билайн, Т2).
- ⚡ **Исправление вылета в Автоподборе стратегий**: решена проблема падения при запуске автоподбора, исправлена передача параметров движку ByeByeDPI (`ciadpi`), добавлена изоляция состояния от работающего VPN-туннеля и обработчик жеста/кнопки "Назад" (`BackHandler`).
- ✈️ **Telegram WS Proxy**: восстановлена работа переключателя вкл/выкл на главном экране (синхронная остановка и освобождение порта), исправлена передача MTProto-секрета (генерация стойкого 32-hex ключа вместо нулей).
- 📱 **Интерфейс выбора приложений**: кнопки выбора приложений в разделах «Свой DNS» и «Анти-DPI» переработаны в полноразмерные удобные кнопки с индикатором количества выбранных пакетов.
- 🔒 **R8/ProGuard и стабильность**: добавлены правила защиты нативных классов JNI и JNA, предотвращающие ошибки линковки в релизной сборке, а также глобальный логгер сбоев (`last_crash.txt`).
